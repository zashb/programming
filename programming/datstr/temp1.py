# 2:
stack=[];if i in "+-*/":op1,op2

# 3
return list(map(s.find,s))==list(map(p.split().index,p.split()))

# 4
currword,currlen=word_ladder.pop(0); part1,part2 = currword[:i],currword[i+1:]

# 6
for j in reversed(range(len(s))):   dp[j+1] = dp[j] and (i == s[j] or i == '?')
for j in range(1, len(s)+1):    dp[j] = dp[j-1] or dp[j]

# 7
init s,e

# 13
stack.append("("); else pop()

# 14
if s[i]+s[i+1]=="()":   s.replace("()","")

# 15
_sum+=nums[j];_min=min(_min,j-i)

# 17
map[i]=map[i-left]=map[i+right] = _sum


# 27:
res[[]]

# 28:
del d[s[low]]
res=max(res,i-low+1)

# 29: hard
missing-=need[s[j]]>0;  need[s[i]]+=1

# 35
l[secret_i]<0 or l[guess_i]>0:  cc+=1
l[secret_i]+=1,l[guess_i]-=1

# 36
h,w=h[s.pop()],i-s[-1]-1
s.append(i)

# 44
res[i][j]=res[i-1][j-1]+res[i-1][j]

# 45
h=min(he[i],he[j]); water=max(water,(j-i)*h)

# 49
d[tuple(sorted(i))].append(i)

# 52
if ranges[-1][-1]+1:    ranges.append([])
ranges[-1][1:]+=[i]

# 53
if i<=first:    first=i;    elif i<=second: second=i

# 55
v=re.finall();  re.sub(,lambda x:v.pop())

# 58
alpha[ord(s(i))-ord("a")]+=1;   alpha[ord(t(i))-ord("a")]-=1;

# 61 and 3
return list(map(s.find,s))==list(map(p.split().index,p.split()))